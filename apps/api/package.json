{
  "name": "@accounting/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev src/index.ts --local",
    "build": "tsc --noEmit",
    "lint": "echo 'No lint rules configured for api'",
    "test": "echo 'No tests configured for api'"
  },
  "dependencies": {
    "@accounting/shared": "workspace:*",
    "@hono/zod-validator": "^0.7.5",
    "hono": "^4.10.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260523.0",
    "typescript": "~6.0.2",
    "wrangler": "^4.68.0"
  }
}
